<form action="<%= notes_url %>" method="post">
   <%= auth_token %>
   <label for="note_content">Write a note:</label>
   <textarea id="note_content" name="note[content]"></textarea>
   <input type="hidden" name="note[track_id]" value="<%= track.id %>">
   <input type="submit">
</form>