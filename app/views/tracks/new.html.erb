<h1>Add a New Track</h1>
<%= render "shared/errors", errors: flash[:errors] if flash[:errors] %>
<%= render "form", track: @track, artist_albums: @track.artist.albums %>

<% content_for :footer do %>
   <%= render "shared/nav", record_directory: [@track.artist, @track.album, "New Track"] %>
<% end %>